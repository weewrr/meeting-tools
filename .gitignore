# Java / Maven
backend/target/
*.class
*.jar

# 数据目录（本地数据不入库）
data/

*.log

# Trae 本地会话 / 配置
.trae/

# 前端构建产物 / 依赖
frontend/dist/
frontend/node_modules/
frontend/*.tsbuildinfo

# 原生前端备份（迁移至 Vue+Vite，保留作回退参考，不入库）
frontend.native/

# LiveKit 服务器二进制（大文件，不入库；按 README 下载后放入 livekit/）
livekit/livekit-server.exe